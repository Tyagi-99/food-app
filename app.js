import React from "react"
import ReactDOM from "react-dom/client"


const heading = <h1>started my program once again : to check my learning</h1>
const root = ReactDOM.createRoot(document.getElementById("root"))
root.render(heading)